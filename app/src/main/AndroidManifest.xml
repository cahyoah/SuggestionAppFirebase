<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="salatiga.pln.forum.adviceapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/tampakdepan"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.SuggestionActivity" />
        <activity android:name=".activity_menu" />
        <activity android:name=".activity_visimisi" />
        <activity android:name=".activity_gambaranumum" />
        <activity android:name=".activity_profileperusahaan" />
        <activity android:name=".activity_sejarahsingkat" />
        <activity android:name=".activity_susunanpegawai"></activity>
    </application>

</manifest>